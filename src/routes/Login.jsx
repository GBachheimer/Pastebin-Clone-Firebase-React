import NavBar from "../components/navbar";

export default function LogIn(props) {
    return (
        <div>
            <NavBar />
            <h1 className = "position-absolute top-50 start-50 translate-middle">Login</h1>
        </div>
       
    );
}