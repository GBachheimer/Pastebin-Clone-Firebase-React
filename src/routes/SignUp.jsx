import NavBar from "../components/navbar";

export default function SignUp(props) {
    return (
        <div>
            <NavBar />
            <h1 className = "position-absolute top-50 start-50 translate-middle">Sign Up</h1>
        </div>
        
    );
}